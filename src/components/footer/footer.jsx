import React from "react";
import styled from "./footer.module.scss";

export default function Footer() {
	return (
		<footer className={styled.footerContainer}>
			<h1 className={styled.footerContent}>Marketplace Nabil Amran</h1>
		</footer>
	);
}
